services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: flag
      POSTGRES_PASSWORD: flag
      POSTGRES_DB: flagdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
